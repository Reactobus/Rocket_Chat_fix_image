# Обновление Rocket.Chat 8.5.0 → 8.6.0 (2026-07-06)

- **Дата:** 2026-07-06
- **Исполнитель:** агент (Cursor)
- **Откуда → куда:** `8.5.0-patched` → `8.6.0-patched`
- **Тип релиза:** GA (8.6.0 только вышел; RC-ветка 8.6.0-rc.x не бралась)
- **Итог:** УСПЕХ
- **Даунтайм:** короткий (пересоздание только `rocketchat`)

## Инфраструктура на момент обновления

- Хост / alias: `srv771799` / `Rocket_chat`
- Каталог: `/home/docker/rocketchat/`
- MongoDB: `8.0` (требование 8.6.0: `["8.0"]`) — апгрейд БД НЕ нужен
- Node (в образе): `22.22.3`
- traefik: `v2.11`

## Алгоритм (по docs/patching.md)

1. Бэкап MongoDB → `bck/rocketchat-before-8.6.0-20260706.dump.gz` (5.1G)
2. `docker pull ...:8.6.0` (перед этим `docker manifest inspect` подтвердил, что GA-тег есть в реестре)
3. Извлечь vanilla `app.js` (старый → `app.js.bak-8.5.0`)
4. Скопировать репо-скрипт, применить → `patched ok (3 блок(ов))` сразу
5. Якоря `NEEDLE_*`: **без изменений** — бандл 8.6.0 идентичен 8.5.0 по структуре, правки скрипта не понадобились
6. `node --check` → SYNTAX_OK
7. Dockerfile `FROM ...:8.6.0`, `docker build --no-cache -t ...:8.6.0-patched`
8. Бэкап compose (`docker-compose.yml.bak-pre-8.6.0`), тег `8.6.0-patched`, `up -d rocketchat --force-recreate`
9. Проверка пройдена

## Проверка результата

- Версия из логов: `8.6.0` (commit `814435ab35`), Node `22.22.3`, MongoDB `8.0.19`
- Маркеры патча в контейнере: `rc-patch:` = 2, `CYR_MAP` = 6
- Функциональный тест (кириллица в имени файла): за пользователем

## Проблемы и решения

- **PowerShell снова сожрал `$(date)`** в имени бэкапа — переименовал на стороне сервера (как и в прошлый цикл).
- **Кавычки в busybox-shell при верификации** (`grep`/`docker exec` через двойные кавычки поломались) — перезапустил проверки отдельными командами с одинарными кавычками.
- **`/api/info` через домен один раз вернул форму с `workspaceUrl`** вместо `version` — версию подтвердил по баннеру `SERVER RUNNING` из `docker logs`.

## Git

- Репозиторий: `Reactobus/Rocket_Chat_fix_image`
- Коммит: `1d79335..bc14411` — Dockerfile `FROM 8.6.0`, обновление README/patching.md (якоря 8.5.0 применились к 8.6.0 без изменений).

## Заметки на следующий цикл

- Якоря стабильны в диапазоне 8.5.0–8.6.0; всё равно проверять `patched ok (3 блок(ов))` и уникальность при следующем бампе.
- Проверять доступность GA-тега `docker manifest inspect` до полноценного `pull`.
- Версию читать из баннера `docker logs` (надёжнее, чем `/api/info` через traefik).
